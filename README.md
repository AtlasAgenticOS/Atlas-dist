# Atlas distribution channel

Public delivery channel for Atlas self-host instances (source stays private). Hosts the update feed (core-version.json), the plugin marketplace (plugin-registry.json + plugins/*.atlasplugin.zip), and the downstream bundle (compose + installer + Caddyfile). Images: ghcr.io/atlasagenticos/atlas-*. See docs/DISTRIBUTION.md in the Atlas project.
